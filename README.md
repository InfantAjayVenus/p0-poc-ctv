# p0-poc-ctv

Monorepo containing the CTV player crash POC frontend and backend.

## Structure

```
web/   # Next.js frontend (player app)
api/   # Express backend (subscriptions API, crash-test diagnostics)
```

See `web/` and `api/README.md` for setup and usage instructions specific to
each app.
